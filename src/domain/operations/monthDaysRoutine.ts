import { Day, Routine, RoutineMoments, Task, TasksList } from '../../types';

export function getMonthDaysRoutineTasks(
	routine: Routine,
	day: Day,
	checks: TasksList
): Array<Task> {
	return [];
}
